import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.stellarjackrabbitbloom',
  appName: 'stellar-jackrabbit-bloom',
  webDir: 'dist'
};

export default config;
